/// 音色播放 - 占位实现（条件导入默认分支，实际不会执行）
library;

import 'tone_player.dart';

void playToneImpl({
  required String name,
  required int octave,
  required ToneType type,
}) {}

void playTickToneImpl({bool accent = false}) {}
